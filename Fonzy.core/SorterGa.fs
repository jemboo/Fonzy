namespace global

type SorterGenome = 
    | Empty
    | B of int

type SorterPhenotype = 
    | Empty
    | B of int

type SorterTestResults = 
    | Empty
    | B of int

type SorterPhenotypeEval = 
    | Empty
    | B of int
